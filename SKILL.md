---
name: cartoon-motion-video-production
description: Produce cartoon motion videos from confirmed copy in the established V55 layout, with on-demand generated and reviewed illustration variations. Use for repeatable production, not a new style or reference exploration.
---

# 卡通动效视频生产

**当前包：按需生图正式版，已于2026-09-16获老大确认启用。** 日常制作不修改 Skill 源码；素材内部验收、视频审核与发布授权按当次任务分别判断，不能把 Skill 启用当成每个产物已批准。

把新稿放入已经认可的 V55 版面：**只换元素，不换版面。** 用户交定稿并要求制作后，Agent 自行读全文、准备音轨与字幕、选图或生图、检查、构建、对比；不要让用户填 JSON。

## 首次安装

先读 [安装与环境检查](references/installation.md)。仓库包含兼容输入模块；不要求安装另一份纸纹 Skill。Mac 字体从本机恢复并校验，配音适配器与个人配置不上传。安装检查区分渲染就绪与新稿配音就绪；缺项时明确告知，不能把安装成功当成可立即全自动出片。

## 不可漂移的成品

所有视频从本包的 scripts/make_video.py 进入。assets/v55/ 是封装好的渲染正本，baseline.lock.json 校验模板、字体和动效；不依赖历史 F30 工程。固定 1280×720、30fps、HyperFrames 0.8.35、绿色纸纹、开场构图、正文插画区域、中文字形/字号/位置、逐句英文和进出场曲线。

- 保留 V55 开场，不自动选择旧 cover-02/03。开场尚未拆成可独立替换的同构图元素；不要把整图替换冒充换背景。
- 正文可换人物、动作、道具和物件组合。使用已校准区域，按可见主体等比例适配，不根据新图改页面、缩小标题或移动字幕。
- 新图不自带页面、纸纹、边框、文字或动画。只挂到原 motion-plane.main .wrap，继承原进场、连续渐进缩放和平移、退出。不得追加第二条 transform 动画或用循环抖动充数。
- 不重画近似模板，不用几何小人、随机网页图片或改变渲染引擎绕过素材缺口。

具体固定/可变边界、尺寸和检查见 [按需素材合同](references/on-demand-assets.md)。

## 路由

| 模式 | 默认使用场景 | 行为 |
|---|---|---|
| diverse-v55 | 新稿默认 | 先检查已验收缓存与近期使用记录；缺少合适或有新鲜度的素材就按需生成，验收后绑定到稿件。开场保持原版。 |
| fixed-v55 | 用户明确要求 V55 原样、做对照回归 | 不读取变化素材，完全保留 V55 插画。 |

新稿不需要再说“我要多样化”。先执行 generated_assets.py plan；没有素材包时 build 会返回 ASSET_GENERATION_REQUIRED，Agent 应接着生图，不能因此结束任务，也不能切 fixed-v55 假装完成多样化。

每片使用冻结的 asset_bundle（路径＋SHA），同一 job 的重建得到同一选择。近期五份不同稿件使用过的候选不再自动复用；素材不够就生成当前需要的几张，不先扩充固定数量。具体内容相关即可，不要求每一句字面精确，但禁止自相矛盾。

## 执行流程

首次执行读 [生产合同与命令](references/production-contract.md)。需要变化素材时再读 [按需素材合同](references/on-demand-assets.md)。

1. doctor 检查模板及依赖；读完整定稿。用户已要求按该稿制作就沿用授权，不重复确认。
2. 已有同稿配音包用 import-paper；新稿用 prepare 后再 import-paper。纸纹 Skill 仅提供已验证的稿件/官方配音/token 时间轴，不提供本片画面或布局。
3. Agent 按真实语音写 visual-plan，导入得到 source-job。diverse-v55 先 plan，再按 brief 调用可用的生图工具。生图工具由 Agent 调用，脚本本身不会凭空访问 ChatGPT 或付费 API。
4. 新素材逐个 check → preview_assets.py 生成未入库预览 → 实际查看原图与 V55 页面 → 记录视觉观察和证据 → admit 到当前任务缓存 → bind 成稿件专属包。技术通过不等于视觉通过；未经查看不得填全通过。
5. 在新目录 build --seconds 30、check --runtime、render、verify。实际查看最终 MP4 的字幕分页、正文运动与切换条带，和同稿 V55 对照。
6. 用户只要样片就交样片。需要完整片时，样片内部检查合格后继续同一 job 的全长构建，使用新输出目录，不把 30 秒叫完整片。
7. record 把已生成候选的使用记录写进任务缓存历史，保留前一份记录；不改变用户批准状态。逐版交 MP4、对比报告、证据和有依据的分项评价。

## 生图工具和失败推进

优先使用所在项目的 ChatGPT Images 工作流；入口连续超时或不可用时，告知后可用内置 imagegen。固定内容目标、主体、比例、V55 参考图、色彩、背景和禁用项，一张图一个资产。保存精确提示词和原图；内置生成不会出现在 chatgpt.com 聊天记录里。

先请求真透明 PNG；如果检查发现棋盘格被画进 RGB，不能称作透明。可改走本包已测试的纯白底 white-key 显示蒙版，只用于彩色、黑粗线、没有重要白色填充的扁平卡通。它不改源 PNG；必须在绿色纸纹上检查无白边/白框、无明显肤色色偏或主体缺损。不支持的情况继续生成或保留缺口，不能偷偷换叠色效果。

同一问题默认只做一次定向重试，再推进其它素材、代码、预览和测试；可以回头用另一种符合合同的背景方案。受阻不代表任务全停。时限到时交实际最佳候选和缺口，不凭空报告成功。

## 验收和状态

- 原稿、配音、真实时间轴、音色和模板哈希必须正确；保持全文，不以摘要代稿。
- 插画连冻超过 0.5 秒、单帧闪屏、跳变、循环抖动均需修复。开场按 V55 的有意停留独立比较。
- build/check 保护文件完整性；verify 只给技术筛查结果，不能证明动效舒服、素材漂亮或平台不会判重复。
- 入库是 Agent 的素材可用性判断；Skill 启用、样片验收与产品发布是不同状态。不得把内部通过写成“老大已批准”。
- 每版比较上版和 V55。评分维度：动效目的25、速度舒适25、连续性20、卡顿闪屏15、字幕音画构图10、规格交付5。没有完整观看/听音或视觉问题未解决时，不报95分以上；可以列暂缓评分，不能靠加分收口。
- 用户本轮时限和制作范围优先。所有新输出保留版本；真实发布按当次授权执行。模板或 Skill 维护先另存候选，验证后按明确确认范围更新正式包。
